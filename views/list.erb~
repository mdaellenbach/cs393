<p>
<a href="/">&laquo; Back</a>
<% end %><p><strong>This is an example of using a loop with a block in an ERB view. The lyrics are generated by a <em>Route</em> in the <code>myapp.rb</code> file, then rendered by the <em>View</em> in <code>views/list.erb</code></strong></p>
<% @dates.each do |line| %>
<p><%= line %></p>
<% end %>